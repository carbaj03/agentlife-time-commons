export type Tool={name:string,description:string,inputSchema:object,annotations:{readOnlyHint:boolean,untrustedContentHint:boolean},execute:(input:unknown)=>Promise<unknown>};
export function registerTools(tools:Tool[]){const context=(document as Document & {modelContext?:{registerTool:(tool:Tool,options:{signal:AbortSignal})=>void|Promise<void>}}).modelContext;if(!context?.registerTool)return;const life=new AbortController();for(const tool of tools){try{void Promise.resolve(context.registerTool(tool,{signal:life.signal})).catch(()=>console.warn('Optional WebMCP registration unavailable'));}catch{console.warn('Optional WebMCP registration unavailable');}}return ()=>life.abort();}
export function object(input:unknown){if(!input||typeof input!=='object'||Array.isArray(input))throw Error('Expected an object');return input as Record<string,unknown>;}
