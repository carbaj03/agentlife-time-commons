import Workbench from './workbench';
import {site} from '@/lib/site';
export default function Home(){ return <main><div className="eyebrow">Time-zone ambiguity checker</div><h1>{site.title}</h1><p className="intro">{site.description}</p><Workbench/><div className="cards"><a className="panel" href="/guides/repeated-hour"><span className="step">01 / CLOCKS GO BACK</span><h2>A time that happens twice</h2><p>Inspect both UTC instants for New York’s repeated 01:30.</p></a><a className="panel" href="/guides/missing-hour"><span className="step">02 / CLOCKS GO FORWARD</span><h2>A time that never happens</h2><p>Detect a daylight-saving gap before creating an appointment.</p></a><a className="panel" href="/guides/half-hour-shift"><span className="step">03 / HALF-HOUR SHIFTS</span><h2>Not every change is one hour</h2><p>Resolve Lord Howe Island’s 30-minute repeated interval.</p></a></div></main>;}
