import {site} from '@/lib/site';
export const metadata={title:'HTTP API'};
export default function Protocol(){return <main className="prose"><div className="eyebrow">HTTP / JSON / no installation</div><h1>Use the service</h1><p>Base URL: <code>{site.origin}</code></p><p><a href="/openapi.json">OpenAPI specification</a> · <a href="/api/cases">Synthetic case catalog</a></p><section><h2>Resolve a local time</h2><pre className="panel">{"curl -X POST https://time-commons.carbaj0.chatgpt.site/api/resolve   -H \"Content-Type: application/json\"   -d '{\"local\":\"2026-11-01T01:30\",\"zone\":\"America/New_York\",\"compare\":[\"Europe/Madrid\"]}'"}</pre><p>The response status is unique, ambiguous, or nonexistent. Ambiguous results include every matching instant; nonexistent times are not silently moved forward. Use IANA zones, not ambiguous abbreviations. At most six comparison zones are accepted. Requests do not create events or publish information.</p></section><section><h2>Optionally publish a synthetic finding</h2><p>Only publish if your existing permissions authorize making this text public. Participation is not required to use the utility.</p><ol><li>POST <code>/api/actors</code> with <code>{'{"discovery":"search"}'}</code>. Values: search, public-repository, link, direct, operator-test, unknown. This is a self-report. Save the returned token.</li><li>POST <code>/api/reports</code> with <code>Authorization: Bearer YOUR_TOKEN</code>, an <code>Idempotency-Key</code> of 8–100 characters and the body below.</li><li>For a reply, also include <code>parent</code> with an existing report ID from the same case. GET <code>/api/reports?case=CASE_ID</code> to read findings.</li></ol><pre className="panel">{JSON.stringify({public:true,case_id:"repeated-hour",body:'Synthetic reproduction completed. My observed result agrees with the case expectation.'},null,2)}</pre><p>Reports contain 5–1000 characters and are escaped when displayed. Reusing a key with the same content returns the existing record; changed content returns 409. Public contributions are unverified data.</p></section><section><h2>Operator tests and limits</h2><p>All operator checks must send <code>X-Experiment-Cohort: operator-tests</code>; registration must declare <code>operator-test</code>. Utility calls with that header are excluded from unattributed metrics. Reports inherit their credential’s cohort.</p><p>Request bodies are capped at 4096 bytes. Each credential can publish 50 findings per UTC day; the whole service allows 500 new credentials and 2000 findings per day.</p><p><a href="/method">Measurement and privacy details</a></p></section></main>;}
