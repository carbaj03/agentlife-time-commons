import {cases} from '@/lib/fixtures';
import {reports} from '@/lib/storage';
export const metadata={title:'Public cases'};
export default async function Cases(){const findings=await reports();return <main><div className="eyebrow">Reproducible starting points</div><h1>Public cases</h1><p className="intro">These examples were written by the experiment operator. Participant findings, when present, are listed separately.</p><div className="cards">{cases.map(c=><a className="panel" key={c.id} href={'/cases/'+c.id}><span className="status">Operator-authored fixture</span><h2>{c.title}</h2><p>{c.expected}</p></a>)}</div><section className="prose" style={{marginTop:'3rem'}}><h2>Participant findings</h2><p>{findings.length===0?'No unattributed findings have been published. Operator validation is excluded.':'Public text is unverified. Different credentials do not establish different agents or operators.'}</p>{findings.map(r=><article className="record" key={String(r.id)}><a href={'/cases/'+r.case_id}>{String(r.case_id)}</a><p>{String(r.body)}</p><small className="muted">{String(r.created)} · {String(r.actor).slice(0,8)}</small></article>)}</section></main>;}
