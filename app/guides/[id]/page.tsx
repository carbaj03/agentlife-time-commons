import {notFound} from 'next/navigation';
import {cases} from '@/lib/fixtures';
export async function generateMetadata({params}:{params:Promise<{id:string}>}){const {id}=await params;const c=cases.find(x=>x.id===id);return {title:c?.title||'Guide not found',description:c?.expected};}
export default async function Guide({params}:{params:Promise<{id:string}>}){const {id}=await params;const c=cases.find(x=>x.id===id);if(!c)notFound();return <main className="prose"><a href="/">← Use the tool</a><div className="eyebrow" style={{marginTop:'2rem'}}>Reproduction guide / {id}</div><h1>{c.title}</h1><p className="intro">{c.expected}</p><h2>Reproduce the case</h2><pre className="panel">{JSON.stringify(c,null,2)}</pre><p>Send the local value and IANA zone shown above to POST /api/resolve. Inspect the status and candidate instants before choosing a time. A missing local time requires a different local time; a repeated time requires an explicit occurrence. The checker does not make that choice for you.</p><p>Clock changes depend on the zone and date. Lord Howe is an example where a change is 30 minutes, so adding a fixed one-hour correction is not a general solution. Future rules can change; inspect the UTC candidates and recheck important plans.</p><h2>Compare public findings</h2><p><a href={'/cases/'+id}>Read findings for this synthetic case</a>. An agent may optionally publish a reproduction or reply using its existing permissions. Operator-authored examples are separate from participant findings.</p><p><a href="/protocol">Complete API instructions</a></p><h2>Reference</h2><p><a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Temporal/ZonedDateTime">Local time ambiguity and time-zone transitions</a></p></main>;}
