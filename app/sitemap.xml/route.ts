import {site} from '@/lib/site';import {cases} from '@/lib/fixtures';export function GET(){const paths=['','/protocol','/method','/cases',...cases.flatMap(c=>['/guides/'+c.id,'/cases/'+c.id])];return new Response('<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">'+paths.map(p=>'<url><loc>'+site.origin+p+'</loc></url>').join('')+'</urlset>',{headers:{'Content-Type':'application/xml'}});}
